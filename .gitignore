# --- large spectra / data (regenerated by src/ downloaders) ---
data/aspcap/
data/dwarfs_train/
data/dr19_dwarfs_train/
data/dr19_sb2/
data/dr19_raw/
data/dr19_raw_sb2/
data/dr19_raw_controls/
data/cache/
data/dr19_continuum_train.npz
# Keep small fixed artifacts (committed): data/binspec_*.npz, data/*mist*.csv, data/*wavelength*.npz
# Raw MIST isochrone source files (bulky; the committed product is the compact
# models/mist_ms_grid.npz built from them -- see data/mist_v1.2/PROVENANCE.md).
data/mist_v1.2/*.iso
data/mist_v1.2/*.iso.cmd
data/mist_v1.2/*.txz

# --- caches / build / deps ---
# Channel-A (SDSS CAS) map cache written by src/download_gaia_local.py; the
# committed product is resources/gaia_dr19_dwarfs.parquet, not this cache.
resources/.gaia_sdss_map_cache.parquet
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
node_modules/
slides/node_modules/
slides/dist/
.DS_Store

# --- secrets (API keys live in ~/.env, never in the repo) ---
.env
*.key

# --- logs / scratch ---
*.log
notebooks/scratch_*.py

# --- bulky regenerable reference material (keep the notes, not the sources) ---
resources/arxiv_sources/

# --- working scratch, not part of the first version (content folded into SKILL.md) ---
research_log.md

# combined spectra parquet (large; regenerate via src/data_io.py convert)
data/dr19_raw.parquet
data/dr19_raw_sb2.parquet
data/dr19_raw_controls.parquet

notebooks/*.html
data/dr19_raw_visits/
tests/functional_report.md

# scratch (AB session)
data_syn_proof/
paper/figures/_*

# internal handoff (kept LOCAL; removed from the shareable tree in AB-5,
# METHODS.md is its public replacement)
PASSDOWN.md

# leftover scaffolding from the halted 272k census (out of scope, not committed)
resources/dr19_census272k_manifest.csv
scripts/census_array272k.sbatch
scripts/run_full272k.sh

# AB-6 local venv (numpy<2 + system torch)
.venv_ab6/
data/val_raw/

# AB-10: cloned reference repos (Ting) -- studied, NEVER imported as weights
resources/ref_repos/
.venv_repro/

# --- internal working notes (not part of the release) ---
llm.txt
NUMBERS_CONTEXT.txt
RESPONSE_TO_REFEREE.md
OSC_RUNS.md
docs/ab18_report_for_yst.md
data/galah_cache/
# retired detector distilled from the non-public reference net; not shipped
models/gap_distill.pt

# --- the manuscript is not part of this release (repo = method + catalog) ---
paper/
paper2/

# --- internal experiment record (kept locally, not part of the release) ---
benchmarks/
report/
figures/
cloud/
METHODS.md
