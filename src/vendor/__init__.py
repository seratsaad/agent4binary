"""Vendored third-party code, copied verbatim into this repo for durability.

Sub-packages here are NOT our work; they are external code copied in so the
pipeline runs without the original clone. Each sub-package carries a header
naming its upstream source and noting that it is vendored verbatim.
"""
